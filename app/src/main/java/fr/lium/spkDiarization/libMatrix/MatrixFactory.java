package fr.lium.spkDiarization.libMatrix;

/**
 * A factory for creating Matrix objects.
 */
public class MatrixFactory {

	/*
	 * static public MatrixSymmetric matrix2SymmetricMatrix(MatrixSquare matrix) { MatrixSymmetric symmetricMatrix = new MatrixSymmetric(matrix.getColumnDimension()); for(int i = 0; i < symmetricMatrix.getSize(); i++) { for(int j = i; j <
	 * symmetricMatrix.getDimension(); j++) { symmetricMatrix.set(i, j, matrix.get(i, j)); } } return symmetricMatrix; } static public MatrixVector matrix2DoubleVector(Matrix matrix) { MatrixVector vector = new
	 * MatrixVector(matrix.getColumnDimension()); for(int i = 0; i < vector.getDimension(); i++) { vector.set(i, matrix.get(0, i)); } return vector; }
	 */

}
